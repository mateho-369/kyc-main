// server.js に追加するルート設定
// 他のルート設定の後、エラーハンドラーの前に追加

// Sharegram SSO routes
const authSharegramSsoRoutes = require('./routes/auth-sharegram-sso');
app.use('/api/v1/auth/sharegram/sso', authSharegramSsoRoutes);

// 確認: この行の前後を確認
// Before: 他のapp.use()ステートメント
// After: エラーハンドラー（404ハンドラーなど）
